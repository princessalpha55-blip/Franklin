.DS_Store
node_modules
*.tsbuildinfo
.env
.env.*
*.log
.gstack/

# Alternate package-manager lockfiles — npm is the source of truth.
# pnpm/yarn lockfiles drift silently because CI only runs `npm ci`.
pnpm-lock.yaml
yarn.lock

# Build output (rebuilt from src on every install)
dist/
.next/

# Local CLI agent settings (user-specific allowlists, not portable)
.claude/settings.local.json
.claude/scheduled_tasks.lock
